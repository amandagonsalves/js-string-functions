const lettersSum = require('./lettersSum');
const removeRepeated = require('./removeRepeated');
const replaceChar = require('./replaceChar');

console.log(lettersSum('ab', 'cd'))
console.log(removeRepeated('xxxzzzwww', 2))
console.log(replaceChar('?te?s?t', 2))

/*
 * You can run console.log() to test the functions here.
 */